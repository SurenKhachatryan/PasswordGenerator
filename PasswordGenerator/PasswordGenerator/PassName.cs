namespace PasswordGenerator
{
    public  class PassName
    {
       public enum ListName
        {
            hexadecimal,
            integer,
            uppercase,
            lowercase,
            simbol,
            allsimbols,
            pincode,
            exit
        }
    }
}
